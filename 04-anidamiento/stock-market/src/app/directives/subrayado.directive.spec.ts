import { SubrayadoDirective } from './subrayado.directive';

describe('SubrayadoDirective', () => {
  it('should create an instance', () => {
    const directive = new SubrayadoDirective();
    expect(directive).toBeTruthy();
  });
});
